# 🗄️ دليل ومخطط جداول قاعدة بيانات النظام (ProSoft SQL Database Dictionary)

يوثق هذا الملف كافة جداول قاعدة بيانات نظام **ProSoft / ChickenDist**، موضحاً اسم كل جدول، ومفتاحه الأساسي (**Primary Key**)، وطبيعة البيانات واستخدامه في النظام المحاسبي والإداري.

---

## 📑 فهرس الأقسام الرئيسية
1. [الأصناف والمخزون والوحدات](#1-الأصناف-والمخزون-والوحدات)
2. [المبيعات ونقاط البيع والأقساط](#2-المبيعات-ونقاط-البيع-والأقساط)
3. [المشتريات والتوريدات والموردين](#3-المشتريات-والتوريدات-والموردين)
4. [العملاء والمديونيات ونقاط الولاء](#4-العملاء-والمديونيات-ونقاط-الولاء)
5. [التصنيع وشجرة المواد (BOM)](#5-التصنيع-وشجرة-المواد-bom)
6. [المالية والخزائن والقيود المحاسبية](#6-المالية-والخزائن-والقيود-المحاسبية)
7. [الموارد البشرية والموظفين والرواتب والورديات](#7-الموارد-البشرية-والموظفين-والرواتب-والورديات)
8. [الأصول الثابتة والشركاء وتوزيع الأرباح](#8-الأصول-الثابتة-والشركاء-وتوزيع-الأرباح)
9. [خدمات الصيانة والسيارات وإعدادات النظام والتزامن السحابي](#9-خدمات-الصيانة-والسيارات-وإعدادات-النظام-والتزامن-السحابي)

---

## 1. الأصناف والمخزون والوحدات

| اسم الجدول | المفتاح الأساسي (PK) | الوصف والاستخدام في النظام |
|---|---|---|
| `Products` | `ProductID` (INT IDENTITY) | الجدول الأساسي لبيانات الأصناف والمنتجات (الأسماء، الأسعار، الباركودات، حد الطلب، الوحدات، أسعار الجملة والقطاعي). |
| `ProductStock` | `StockID` (INT IDENTITY) | أرصدة المنتجات الموزعة على المخازن المختلفة مع قيد فريد `(ProductID, WarehouseID)`. |
| `ProductBatches` | `BatchID` (INT IDENTITY) | تشغيلات ودفعات المنتجات ذات تواريخ الصلاحية وتتبع الكميات لكل تاريخ انتهاء. |
| `Warehouses` | `WarehouseID` (INT IDENTITY) | بيانات المستودعات والمخازن التابعة للشركة وفروعها. |
| `Categories` | `CategoryID` (INT IDENTITY) | تصنيفات وأقسام المنتجات وقطع الغيار لتنظيم شجرة الأصناف. |
| `Units` | `UnitID` (INT IDENTITY) | قائمة وحدات القياس (قطعة، علبة، كرتونة، كيس، كيلو، متر، جوز). |
| `ProductSizes` | `SizeID` (INT IDENTITY) | مقاسات وأحجام المنتجات (للأنشطة التي تعتمد المقاسات). |
| `Colors` | `ColorID` (INT IDENTITY) | درجات وألوان المنتجات. |
| `Brands` | `BrandID` (INT IDENTITY) | الماركات التجارية والشركات المصنعة للأصناف. |
| `CarModels` | `CarModelID` (INT IDENTITY) | موديلات وماركات السيارات المتوافقة مع قطع الغيار. |
| `ShelfLocations` | `ShelfID` (INT IDENTITY) | أماكن التخزين والرفوف والممرات داخل المخازن لتسريع جمع البضاعة. |
| `ProducerCompanies` | `ProducerID` (INT IDENTITY) | الشركات المصنعة والمستوردة للمنتجات. |
| `WarehouseTransfers` | `TransferID` (INT IDENTITY) | رأس إذن التحويل المخزني بين المخازن (تاريخ، مخزن المصدر، مخزن الوجهة، الحالة). |
| `WarehouseTransferItems` | `ItemID` (INT IDENTITY) | تفاصيل وبنود الأصناف والكميات المحولة بين المخازن. |
| `StockAdjustments` | `AdjID` (INT IDENTITY) | تسويات الجرد المخزني وفرق الرصيد الدفتري عن الفعلي. |
| `WastageLoss` | `WastageID` (INT IDENTITY) | أذونات الهالك والتالف وتوالف النقل والتشغيل. |
| `WastageLossItems` | `ItemID` (INT IDENTITY) | بنود وأصناف وتكاليف الهالك المسجلة في إذن التالف. |
| `ShortageNotebook` | `ShortageID` (INT IDENTITY) | كشكول النواقص لتسجيل ومتابعة الأصناف الناقصة وتحديد كميات الطلب وحالة التوفير. |
| `PriceChangesLog` | `LogID` (INT IDENTITY) | سجل تدقيق التغيرات في أسعار البيع والشراء وتاريخ التعديل ومن قام به. |

---

## 2. المبيعات ونقاط البيع والأقساط

| اسم الجدول | المفتاح الأساسي (PK) | الوصف والاستخدام في النظام |
|---|---|---|
| `Sales` | `SaleID` (INT IDENTITY) | رأس فواتير المبيعات ونقاط البيع (العميل، الإجمالي، المدفوع، الآجل، طريقة الدفع، الخصم، الوردية). |
| `SaleItems` | `ItemID` (INT IDENTITY) | تفاصيل وبنود فاتورة المبيعات (الصنف، الكمية، السعر، المعامل، الخصم، الباركود). |
| `SalesReturns` | `ReturnID` (INT IDENTITY) | رأس فواتير مرتجعات المبيعات المستلمة من العملاء. |
| `ReturnItems` | `ItemID` (INT IDENTITY) | بنود الأصناف المرتجعة وأسعارها وكمياتها. |
| `SalesAudit` | `AuditID` (INT IDENTITY) | سجل المراجعة والتدقيق للتعديلات التي تتم على فواتير المبيعات بعد حفظها. |
| `SaleItemsHistory` | `HistoryID` (INT IDENTITY) | لقطة تاريخية لبنود الفاتورة قبل التعديل لأغراض الرقابة المحاسبية. |
| `PriceQuotes` | `QuoteID` (INT IDENTITY) | عروض الأسعار المقدمة للعملاء والشركات. |
| `PriceQuoteItems` | `QuoteItemID` (INT IDENTITY) | بنود وأصناف عرض السعر وتفاصيل الخصومات. |
| `CustomerReservations` | `ReservationID` (INT IDENTITY) | حجوزات العملاء المسبقة والدفعات المقدمة والمواعيد المتوقعة للتسليم. |
| `CustomerReservationItems` | `ItemID` (INT IDENTITY) | بنود الأصناف المحجوزة للعملاء. |
| `InstallmentContracts` | `ContractID` (INT IDENTITY) | عقود التقسيط الشرعي (بيانات العميل، قيمة العقد، المقدم، عدد الأقساط، الحالة). |
| `InstallmentSchedules` | `ScheduleID` (INT IDENTITY) | جدول ومواعيد استحقاق أقساط العقد (رقم القسط، تاريخ الاستحقاق، المبلغ، حالة السداد). |
| `InstallmentPayments` | `PaymentID` (INT IDENTITY) | سجل سدادات وتحصيلات الأقساط والخزينة المستلمة ورقم الإيصال. |
| `InstallmentAuditLog` | `LogID` (INT IDENTITY) | سجل التعديلات والعمليات على عقود وجداول الأقساط. |

---

## 3. المشتريات والتوريدات والموردين

| اسم الجدول | المفتاح الأساسي (PK) | الوصف والاستخدام في النظام |
|---|---|---|
| `Suppliers` | `SupplierID` (INT IDENTITY) | دليل الموردين والشركات الموردة وأرصدتهم الافتتاحية وبيانات الاتصال. |
| `SupplierTransactions` | `TransID` (INT IDENTITY) | كشف حساب الموردين (مدين/دائن/فواتير/دفعات وسدادات). |
| `Purchases` | `PurchaseID` (INT IDENTITY) | رأس فواتير المشتريات والتوريدات (المورد، الإجمالي، الخصم، الضريبة، تكلفة الشحن، رقم فاتورة المورد). |
| `PurchaseItems` | `ItemID` (INT IDENTITY) | تفاصيل بنود وأصناف فاتورة الشراء وتكلفة الوحدة والكمية الموردة. |
| `PurchaseReturns` | `ReturnID` (INT IDENTITY) | رأس فواتير مرتجع المشتريات المردودة إلى الموردين. |
| `PurchaseReturnItems` | `RItemID` (INT IDENTITY) | بنود الأصناف المرتجعة للموردين. |

---

## 4. العملاء والمديونيات ونقاط الولاء

| اسم الجدول | المفتاح الأساسي (PK) | الوصف والاستخدام في النظام |
|---|---|---|
| `Clients` | `ClientID` (INT IDENTITY) | بيانات العملاء والشركات والحد الائتماني والمديونية ونقاط الولاء ونوع السعر الافتراضي. |
| `ClientTransactions` | `TransID` (INT IDENTITY) | كشف حساب العملاء التفصيلي (فواتير، دفعات، خصومات، إيصالات قبض). |
| `ClientCratesTransactions` | `CrateTransID` (INT IDENTITY) | تتبع عهدة الصناديق والأقفاص وحركاتها مع العملاء (وارد / صادر / رصيد العهدة). |
| `LoyaltyTransactions` | `TransID` (INT IDENTITY) | سجل حركات نقاط الولاء للعملاء (اكتساب النقاط عند الشراء واستبدالها). |

---

## 5. التصنيع وشجرة المواد (BOM)

| اسم الجدول | المفتاح الأساسي (PK) | الوصف والاستخدام في النظام |
|---|---|---|
| `BOMHeader` | `BOMID` (INT IDENTITY) | رأس وصفة التصنيع وشجرة المنتج النهائي ومعدل الإنتاج المعياري. |
| `BOMItems` | `BOMItemID` (INT IDENTITY) | المواد الخام والمكونات الداخلة في تركيبة كل وحدة مصنعة ونسب الاستهلاك. |
| `ProductionOrders` | `ProductionID` (INT IDENTITY) | أوامر وتشغيلات التصنيع (المنتج النهائي، الكمية المصنعة، تكلفة الخامات، المصاريف الإضافية، التكلفة النهائية، الحالة). |
| `ProductionOrderItems` | `ItemID` (INT IDENTITY) | المواد الخام الفعلية المصروفة والمستهلكة في أمر الإنتاج. |
| `ProductionOrderHistory` | `HistoryID` (INT IDENTITY) | سجل مراحل وتاريخ تنفيذ أمر الإنتاج والمسؤولين. |

---

## 6. المالية والخزائن والقيود المحاسبية

| اسم الجدول | المفتاح الأساسي (PK) | الوصف والاستخدام في النظام |
|---|---|---|
| `SafeAccounts` | `AccountID` (INT IDENTITY) | دليل الخزائن الرئيسية والفرعية والحسابات البنكية وحسابات نقاط البيع الإلكترونية (فيزا). |
| `CashBox` | `CashID` (INT IDENTITY) | سجل حركة النقدية اليومية (إيرادات، مصروفات، تحصيلات، توريدات بنكية، سحوبات). |
| `Expenses` | `ExpenseID` (INT IDENTITY) | المصروفات الإدارية والتشغيلية والعمومية وتصنيفاتها. |
| `ExpenseTypes` | `ExpenseTypeID` (INT IDENTITY) | دليل بنود وأنواع المصروفات (رواتب، إيجار، وقود، صيانة، تسويق...). |
| `JournalEntries` | `EntryID` (INT IDENTITY) | رأس قيود اليومية العامة المزدوجة (تاريخ، وصف القيد، المصدر، المسؤول). |
| `JournalDetails` | `DetailID` (INT IDENTITY) | أطراف القيد المحاسبي المزدوج (اسم الحساب، المدين، الدائن). |
| `AccountingAdjustments` | `AccountKey` (NVARCHAR(100)) | الأرصدة الافتتاحية للميزانية العمومية والحسابات الختامية (أصول، خصوم، حقوق ملكية). |

---

## 7. الموارد البشرية والموظفين والرواتب والورديات

| اسم الجدول | المفتاح الأساسي (PK) | الوصف والاستخدام في النظام |
|---|---|---|
| `Employees` | `EmpID` (INT IDENTITY) | بيانات الموظفين والكاشيرية والمناديب والرواتب وكلمات المرور والصلاحيات. |
| `Permissions` | `PermID` (INT IDENTITY) | شاشات وصلاحيات الوصول والعمليات المتاحة لكل موظف/مستخدم. |
| `EmployeeAttendance` | `AttendanceID` (INT IDENTITY) | سجل الحضور والانصراف اليومي وساعات العمل والتأخير والغياب والإضافي. |
| `EmployeeSalaryItems` | `ItemID` (INT IDENTITY) | بنود البدلات والمكافآت والخصومات والسلف والقروض الخاصة بكل موظف. |
| `EmployeeProductCommissions` | `RuleID` (INT IDENTITY) | نسب ومبالغ عمولات البيع المحددة للمناديب على أصناف معينة. |
| `EmployeeCommissionTiers` | `TierID` (INT IDENTITY) | شرائح التارجت وعمولات تحقيق مستهدف المبيعات. |
| `EmployeeMonthlyPayroll` | `PayrollID` (INT IDENTITY) | مسيرات الرواتب الشهرية المعتمدة وتفاصيل صافي الراتب والخزينة المصروف منها. |
| `EmployeeTransactions` | `TransID` (INT IDENTITY) | كشف حساب حركات الموظف (سلف، عجز مالي، تسويات). |
| `Shifts` | `ShiftID` (INT IDENTITY) | إدارة ورديات الكاشير ونقاط البيع (وقت الفتح، الإغلاق، رصيد الدرج، المبيعات النقدية، الفيزا، العجز/الزيادة). |
| `DriverLoads` | `LoadID` (INT IDENTITY) | أذونات تحميل وتفريغ بضاعة سيارات التوزيع مع المناديب والسائقين. |
| `DriverLoadItems` | `ItemID` (INT IDENTITY) | أصناف وكميات البضاعة المحملة لسيارة المندوب. |
| `DriverHandovers` | `HandoverID` (INT IDENTITY) | محاضر تصفية وجرد رحلة المندوب (المبيعات، المرتجعات، التوالف، النقدية الموردة). |
| `HandoverItems` | `ItemID` (INT IDENTITY) | تفاصيل المرتجع والمباع في محضر تصفية المندوب. |

---

## 8. الأصول الثابتة والشركاء وتوزيع الأرباح

| اسم الجدول | المفتاح الأساسي (PK) | الوصف والاستخدام في النظام |
|---|---|---|
| `FixedAssetCategories` | `CategoryID` (INT IDENTITY) | تصنيفات الأصول الثابتة (أراضي، مباني، آلات، سيارات، أجهزة، أثاث) ونسب الإهلاك. |
| `FixedAssets` | `AssetID` (INT IDENTITY) | بطاقة الأصل الثابت (تاريخ الشراء، التكلفة، العمر الإنتاجي، مجمع الإهلاك، القيمة الدفترية). |
| `FixedAssetDepreciations` | `DepreciationID` (INT IDENTITY) | أقساط وسجلات الإهلاك المحاسبي الدوري للأصل. |
| `FixedAssetOperations` | `OperationID` (INT IDENTITY) | عمليات الأصل من صيانة، استبعاد، بيع، تخريد، أو إعادة تقييم. |
| `Partners` | `PartnerID` (INT IDENTITY) | دليل الشركاء والمساهمين ونسب الحصص في رأس المال والأرصدة الجارية. |
| `PartnerTransactions` | `TransID` (INT IDENTITY) | كشف حساب الشريك (مسحوبات شخصية، إيداعات، نصيب في الأرباح). |
| `DividendDistributions` | `DistributionID` (INT IDENTITY) | جلسات وقرارات توزيع الأرباح الدورية وصافي الربح الموزع والمحتجز. |
| `DividendDistributionLines` | `LineID` (INT IDENTITY) | نصيب كل شريك من الأرباح الموزعة وحالة الصرف والسداد. |

---

## 9. خدمات الصيانة والسيارات وإعدادات النظام والتزامن السحابي

| اسم الجدول | المفتاح الأساسي (PK) | الوصف والاستخدام في النظام |
|---|---|---|
| `MaintenanceTickets` | `TicketID` (INT IDENTITY) | بطاقات وتذاكر صيانة الأجهزة والسيارات وتكاليف قطع الغيار والمصنعيات وحالة الجهاز. |
| `Vehicles` | `VehicleID` (INT IDENTITY) | دليل سيارات ومركبات التوزيع والنقل وأرقام اللوحات والمصروفات المرتبطة بها. |
| `AppSettings` | `SettingKey` (NVARCHAR(100)) | إعدادات وتفضيلات النظام المخزنة بنظام (مفتاح / قيمة). |
| `CloudSyncSettings` | `SettingID` (INT) | إعدادات الربط السحابي ومزامنة البيانات مع تطبيق المالك والموبايل وFirebase. |
| `versions` | `version` (NVARCHAR(50)) | رقم إصدار قاعدة البيانات والنسخة التنفيذية المعتمدة للشبكة المحلية. |
