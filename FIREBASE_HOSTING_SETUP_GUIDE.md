# 🚀 دليل إنشاء وربط سيرفر Firebase مخصص واستضافة تطبيق المالك (Firebase Hosting) لكل عميل

يوفر هذا الدليل شرحاً مبسطاً خطوة بخطوة لطريقة إنشاء سيرفر/مشروع مخصص على **Google Firebase** مجاناً لكل عميل، ورفع واستضافة تطبيق المالك عليه بضغطة زر.

---

## 🌟 مميزات طريقة Firebase المخصصة لكل عميل
1. **استقلالية تامة وأمان 100%:** كل عميل لديه مشروعه وسيرفره الخاص وقاعدة بياناته المستقلة على فيربيز.
2. **مجاني مدى الحياة (Spark Plan):** استضافة Firebase Hosting وقواعد Realtime Database مجانية تماماً وبدون أي اشتراكات.
3. **سرعة فائقة واستقرار دائم:** لا يعتمد على IP أو أجهزة مفتوحة محلياً؛ التقارير تصل للموبايل في جزء من الثانية من أي مكان في العالم.
4. **تطبيق PWA للأندرويد والآيفون:** يُثبت كأيقونة تطبيق حقيقية على شاشة الهاتف الرئيسية مع دعم الوضع الليلي والفاتح والتنبيهات.

---

## 🛠️ الخطوة 1: إنشاء مشروع Firebase جديد للعميل (يستغرق دقيقتين)

1. افتح موقع [Firebase Console](https://console.firebase.google.com/) وسجل الدخول بحساب Gmail.
2. اضغط على **Add project (إنشاء مشروع جديد)**.
3. اكتب اسم المشروع للعميل (مثال: `alrahma-group` أو `al-salam-store`)، ثم اضغط **Continue**.
4. (اختياري) يمكنك إلغاء تفعيل Google Analytics ثم الضغط على **Create project**.
5. انتظر ثوانٍ حتى يتم تجهيز المشروع ثم اضغط **Continue**.

---

## 🗄️ الخطوة 2: تفعيل قواعد البيانات (Realtime Database & Firestore)

### أ. تفعيل Realtime Database:
1. من القائمة الجانبية في فيربيز، اختر **Build** ثم **Realtime Database**.
2. اضغط **Create Database**، اختر الموقع الافتراضي، ثم اضغط **Next**.
3. اختر **Start in test mode** ثم اضغط **Enable**.

### ب. تفعيل Firestore:
1. من القائمة الجانبية، اختر **Firestore Database** واضغط **Create database**.
2. اختر **Start in test mode** ثم اضغط **Enable**.

---

## 🚀 الخطوة 3: نشر واستضافة تطبيق المالك (Firebase Hosting)

يوجد داخل مجلد البرنامج `MobileApp` سكريبت نشر تلقائي سريع:
1. افتح المجلد: `D:\prosoft\MobileApp` أو مجلد البرنامج المكتبي `MobileApp`.
2. شغّل الملف: **`deploy_client_firebase.bat`**.
3. سيطلب منك إدخال `Firebase Project ID` (الذي أنشأته في الخطوة 1 مثل: `alrahma-group`).
4. اضغط **Enter** وسيقوم برفع واستضافة التطبيق وقواعد الأمان تلقائياً خلال ثوانٍ.

> 🌐 **رابط تطبيق المالك للعميل سيكون:**  
> `https://<Firebase-Project-ID>.web.app`  
> (مثال: `https://alrahma-group.web.app`)

---

## 💻 الخطوة 4: ربط البرنامج المكتبي بالمشروع

1. افتح برنامج الكاشير والمبيعات.
2. من القائمة أو الشاشة الرئيسية، افتح: **📱 ربط وتزامن تطبيق الموبايل (Firebase)**.
3. في خانة **معرّف مشروع فيربيز (Project ID)** أدخل اسم المشروع (مثال: `alrahma-group`).
4. فعّل خيار **المزامنة اللحظية مع كل حركة بيع**.
5. اضغط **💾 حفظ الإعدادات** ثم اضغط **🔥 مزامنة وتحديث بيانات المالك الآن**.

---

## 📱 الخطوة 5: تشغيل وتثبيت التطبيق على موبايل المالك

1. افتح الرابط `https://<Firebase-Project-ID>.web.app` من متصفح الموبايل (Chrome على أندرويد أو Safari على آيفون).
2. سيتصل التطبيق فوراً وتظهر كافة تقارير المبيعات والأرباح والخزنة والمخزن والنواقص لحظياً.
3. لتثبيت التطبيق كـ أيقونة على شاشة الموبايل:
   - **في أندرويد:** اضغط زر القائمة (⋮) ثم اختر **إضافة إلى الشاشة الرئيسية (Add to Home screen)** أو **تثبيت التطبيق**.
   - **في آيفون:** اضغط زر المشاركة (Share) في الأسفل ثم اختر **إضافة إلى الشاشة الرئيسية (Add to Home Screen)**.
